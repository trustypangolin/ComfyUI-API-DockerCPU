# ComfyUI-API-DockerCPU Requirements
# Install with: pip install -r requirements.txt

# API Clients
replicate>=1.0.7
fal-client>=0.5.0
huggingface_hub>=0.20.0

# Audio processing
soundfile>=0.12.0

# Image processing
Pillow>=9.0.0

# Configuration
PyYAML>=6.0

# PyTorch (if not already installed with ComfyUI)
# These are typically included with ComfyUI, but listed for reference
# torch>=2.0.0
# torchvision>=0.15.0
# torchaudio>=2.0.0

# Development dependencies
pytest>=7.0.0
pytest-cov>=4.0.0
